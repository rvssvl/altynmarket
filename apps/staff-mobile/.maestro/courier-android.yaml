# Per-platform wrapper: iOS and Android EAS jobs run concurrently, each with
# its own staff account so they never race for the same task.
appId: kz.altynmarket.staff.demo
---
- runFlow:
    file: _courier-steps.yaml
    env:
      PHONE: "+77000000003"
